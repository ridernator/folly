package com.rider.folly.json.enums;

/**
 *
 * @author Ciaron Rider
 */
public enum SortDir {
    /**
     * Order from earliest value to latest e.g. lowest betId is first in the
     * results.
     */
    EARLIEST_TO_LATEST,
    /**
     * Order from the latest value to the earliest e.g. highest betId is first
     * in the results.
     */
    LATEST_TO_EARLIEST

}
